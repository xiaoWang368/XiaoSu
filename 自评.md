# 自评

## 做了什么、解决谁的什么问题

我做了一个公司内部 AI 助手「小苏」:

- **给员工**:在钉钉群 @ 或 Web 聊天页提问——查制度(年假/报销/入职)得到**带原文引用**的答案;查系统数据(员工/考勤/订单/时间)由模型**自主决定调工具**;检索不到明确拒答、不瞎编。保证准确性的同时减少员工翻阅复杂资料的时间，提高工作效率
- **给管理员**:Web 管理后台上传/删除知识库文档、看索引状态、看对话日志(工具调用/Token 消耗/拒答)。

## 最得意的一个技术决策

**查询门面(QueryService)作为 IM 与 Web 的唯一入口,配合 LLM 意图路由**:

```
IM 回调 ─┐
Web SSE ─┴→ QueryService → LangGraph 查询工作流
                              route(LLM 判断) → knowledge(检索) / tool(工具) / chat(闲聊) / refuse(拒答)
```

好处是:钉钉/Web 只差一个"收发层",业务全复用;面试时"加一个工具"只需在 `node_tool_agent` 加一个 schema 和 executor。这条链路把笔试的"模型自主决策、引用溯源、多轮、拒答、流式"都串了起来。

## 一个被迫的妥协

**嵌入从语义模型(本地 BGE-M3 / DashScope)妥协成了"确定性本地嵌入"**(jieba 词级 + 字符 bigram ):

- 为了**任何机器离线可跑、导入/查询向量空间 100% 一致**(之前 DashScope 时好时坏导致检索 0 条,很惨);
- 代价是检索是"关键词型"不是"语义型"——措辞大改的问题可能命中不准,且嵌入质量弱于语义模型。

## 如果再给我 1 周,我会优先

1. 在导入管线加llm视觉模型识别图片，并在其中用滑动窗口+错误重试+超时熔断作为限流机制
2. 写 Evals 评测脚本(≥20 条 case 跑准确率)——定量暴露 LLM"发挥过头"(如报销答出资料外的"OA 打印"细节),再针对性调 prompt / 换模型
3. 将其做成mcp，供claude code或codex使用
