1. #### 用了哪些 AI 工具（Claude Code / Cursor / Codex / 其他）？分别用在哪些环节：

   用的ai工具是claude code,先让ai编写项目实现计划书，然后按照已有思路创建框架、编写代码,增添异常捕获,编写测试用例，校验数据类型以及bug调试。没有直接给ai一个笔试题要求就让它自己实现功能，而是和它确定技术方案，所用的数据库类型、存储方式、检索方式等。开不同的窗口，前一个ai写的代码给另外一个ai查看，看有没有bug，配合我前后端联调。

2. #### 举一个具体的例子：你给 AI 的 prompt 是什么、它生成的代码哪里能用、哪里你必须改、为什么改？

   prompt实例：”按照项目计划书，下一步的行动是什么，你实现的具体思路是什么，可满足什么样的需求。“  确定好之后“好的，开始完成这个需求，不要动无关的代码、虚构不存在的类和方法，尽量沿用已有的方法和类”。  ai对于一些重复性高的代码一般不会出错，像异常捕获、对象的基础类型解析与创建这一类代码可复用，但对于逻辑性较强和架构紧密的代码容易出错，比如这次ai直接将检索调用放在了同步函数里面，导致回答慢，有时对于钉钉stream会超时。

3. #### 有没有一次 AI 把你带沟里去的经历？你怎么发现的、怎么收拾的？

   有的，这次项目用的langgraph框架，ai在编写检索管线的时候，直接将所有文档里不存在的消息全部取决回答，并且对于已有的信息回答太人机，发现是入口节点路由逻辑有问题，并且温度调的太低，对于闲聊等用户信息，直接路由到了end节点。发现之后依然是给ai指出问题后让它来编写代码。

4. #### 你怎么验证 AI 生成的代码是对的？

   1.写出来的代码先review，测试没问题之后看代码答题逻辑和确定的是否一致、边界情况

   2.减少ai一次性的任务复杂度，将任务拆解再执行

   3.核对官方文档

5. #### 如果让你再做一遍，你会怎么调整 AI 的使用方式？

   1.对于review代码，我这次只看了少数情况，对于state命名不对、信息没有到库情况没有注意，下次会多加注意这些细节问题

   2.中间变量打印日志，能了解程序运行情况

   3.让ai写代码之前会先和它确认实现方案和思路，到具体哪一步的时候再跟他说更明确的思路。